# We keep config.js in git because:
# 1. The URL is obfuscated (base64)
# 2. It's needed for GitHub Pages deployment
# 3. Server-side validation protects against abuse
# 4. Anyone can find it via DevTools anyway

# If you need local overrides, create config.local.js
config.local.js

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~
